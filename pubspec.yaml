name: khana_khassi
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.7.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  carousel_slider: ^4.2.1
  carousel_pro: ^1.0.0
  firebase_core: ^2.7.0
  cloud_firestore: ^4.4.3 #^0.13.7
  firebase_auth: ^4.2.9 #^0.16.1
  provider: ^6.0.5
  uuid: ^3.0.7
  transparent_image: ^2.0.1
  flutter_spinkit: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
assets:
  - assets/
